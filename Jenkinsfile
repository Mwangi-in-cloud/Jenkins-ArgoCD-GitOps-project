pipeline {
    agent any
    environment {
        IMAGE = "docker.io/cronosm4m/pipeline"
        TAG = "$(env.BUILD_NUMBER)"
    }
    stages{
        stage (checking out ) {
            steps {
                echo "cheking out"
            }
        }
    }
}